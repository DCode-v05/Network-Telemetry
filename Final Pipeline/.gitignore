# python
__pycache__/
*.pyc

# c build artifacts
c/build/*.exe
c/build/*.o
c/build/parity_data.txt

# dashboard
dashboard/node_modules/
dashboard/dist/
dashboard/public/data/*.json

# keep the results the pipeline generates out of the way if desired
# results/*.json
